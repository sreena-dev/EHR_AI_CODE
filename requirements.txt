# AIRA Healthcare — Production Requirements (Render Free Tier)
# Python 3.11.9 (set via PYTHON_VERSION env var on Render)
#
# Strategy: Core web serving + lightweight AI deps only.
# Heavy ML packages (PaddleOCR, PyTorch, pyannote) are commented out.
# The core app (auth, patient records, billing, vitals, OCR via Tesseract) works without them.

# ── Web Framework ────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.27.0
starlette>=0.36.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# ── Auth & Security ──────────────────────────────────────────────
PyJWT>=2.8.0
bcrypt>=4.1.0
cryptography>=42.0.0

# ── Database ─────────────────────────────────────────────────────
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# ── Lightweight OCR (Tesseract — no GPU needed) ──────────────────
pytesseract>=0.3.10
pillow>=10.0.0
opencv-python-headless>=4.8.0
pdf2image>=1.17.0
pdfplumber>=0.10.0

# ── Speech (Faster-Whisper — uses ctranslate2, NOT PyTorch) ─────
faster-whisper>=1.0.0
# ctranslate2 is pulled automatically by faster-whisper

# ── NLP & HuggingFace ────────────────────────────────────────────
# Note: do NOT pin huggingface_hub here — tokenizers<1.0 constraint must be respected
tokenizers>=0.15.0
langdetect>=1.0.9

# ── Image / audio processing ─────────────────────────────────────
numpy>=1.24.0
soundfile>=0.12.0

# ── Utilities ────────────────────────────────────────────────────
httpx>=0.26.0
requests>=2.31.0
pydantic>=2.5.0
anyio>=4.0.0
tqdm>=4.65.0
python-dateutil>=2.8.0
packaging>=23.0

# ── OPTIONAL HEAVY ML (comment out for Render free tier) ─────────
# Uncomment ONLY if running on a server with 8GB+ RAM
#
# paddlepaddle==2.6.2          # PaddleOCR backend (heavy, ~500MB)
# paddleocr==2.8.1             # Table/layout OCR
# torch>=2.1.0                 # PyTorch (heavy, ~2GB)
# torchaudio>=2.1.0
# transformers>=4.35.0         # BERT/BioClinicalBERT
# pyannote-audio>=3.1.0        # Speaker diarization