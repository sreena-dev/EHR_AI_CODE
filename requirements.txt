# AIRA Healthcare — Production Requirements (Render Free Tier)
# Python 3.11.9 (see runtime.txt)
#
# Strategy: Core web serving + lightweight AI deps only.
# Heavy ML packages (PaddleOCR, PyTorch, pyannote) are installed
# as optional extras and loaded lazily — they are NOT needed at startup.
# The core app (auth, patient mgmt, billing, vitals, OCR via Tesseract) works without them.

# ── Web Framework ────────────────────────────────────────────────
fastapi==0.128.0
uvicorn==0.40.0
starlette==0.50.0
python-multipart==0.0.22
python-dotenv==1.2.1

# ── Auth & Security ──────────────────────────────────────────────
PyJWT==2.11.0
bcrypt==5.0.0
cryptography==46.0.0

# ── Database ─────────────────────────────────────────────────────
SQLAlchemy==2.0.46
psycopg2-binary>=2.9.9
alembic==1.15.0

# ── Lightweight OCR (Tesseract — no GPU needed, ~50MB) ───────────
pytesseract==0.3.13
pillow==10.4.0
opencv-python-headless==4.10.0.84
pdf2image==1.17.0
pdfplumber==0.11.4

# ── Speech (Faster-Whisper — uses ctranslate2, NOT PyTorch) ─────
faster-whisper==1.1.1
ctranslate2==4.4.0

# ── NLP (lightweight — HuggingFace Transformers without torch) ──
huggingface_hub==1.2.0
tokenizers==0.21.0
langdetect==1.0.9

# ── Image processing ─────────────────────────────────────────────
numpy==1.26.4

# ── Utilities ────────────────────────────────────────────────────
httpx==0.27.0
requests==2.32.3
pydantic==2.7.4
anyio==4.4.0
tqdm==4.66.4
python-dateutil==2.9.0.post0
packaging==24.1
soundfile==0.12.1

# ── OPTIONAL HEAVY ML (comment out for Render free tier) ─────────
# Uncomment below ONLY if running on a server with 8GB+ RAM
# These are lazily loaded — app will work without them, just with degraded AI features
#
# paddlepaddle==2.6.2          # PaddleOCR backend (heavy, ~500MB)
# paddleocr==2.8.1             # Table/layout OCR (requires paddlepaddle)
# torch==2.3.1                 # PyTorch (heavy, ~2GB — needed for NLP BERT models)
# torchaudio==2.3.1            # Audio processing (requires torch)
# transformers==4.41.2         # BERT/BioClinicalBERT (requires torch for inference)
# pyannote-audio==3.3.2        # Speaker diarization (requires torch + lots of RAM)
# pyannote-pipeline==3.0.1
# pyannote-core==5.0.0